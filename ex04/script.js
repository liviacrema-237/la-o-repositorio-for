let totalNotas = 0;
for(let i = 1; i <=5; i++) {
    let nota = Number(prompt("Digite a " + i + "ª nota:"));
}
totalNotas += nota;
let media = totalNota /5;
alert ("A media das notas é:" + media.toFixed(2));
